# ==========================================
# Care Home Backend - Application Properties
# ==========================================

# Server
server.port=8080

# ==========================================
# H2 In-Memory Database (Local Development)
# ==========================================
spring.datasource.url=jdbc:h2:mem:carehomedb;DB_CLOSE_DELAY=-1;MODE=Oracle
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# ==========================================
# Oracle Production Database (Uncomment for prod)
# ==========================================
# spring.datasource.url=jdbc:oracle:thin:@localhost:1521:XE
# spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
# spring.datasource.username=carehome_user
# spring.datasource.password=your_secure_password
# spring.sql.init.mode=never

# ==========================================
# JWT Configuration
# ==========================================
jwt.secret=CareHome@SuperSecretKey2024!XYZ987abcdefghijklmnopqrstuvwxyz12345678
jwt.expiration.ms=28800000

# ==========================================
# CORS
# ==========================================
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://adarshmalayath.github.io,https://cet-circus-lane-greatest.trycloudflare.com

# ==========================================
# Logging
# ==========================================
logging.level.com.carehome=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.jdbc=DEBUG

# ==========================================
# Mail Configuration (Gmail SMTP)
# ==========================================
# To enable email: create a Gmail App Password at:
# https://myaccount.google.com/apppasswords
# Then set MAIL_PASSWORD=your_app_password (16 chars, no spaces)

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=adarshmspamemails@gmail.com
spring.mail.password=euqlitjqhitploae
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Sender display name/address used in From header
app.mail.from=CareHome Services <adarshmspamemails@gmail.com>
# Set to false to disable email sending (e.g. in CI or without MAIL_PASSWORD)
app.mail.enabled=${MAIL_ENABLED:true}
